/*Create an array with random number, then print out every element of that array*/
let randomNumbers = [34, 7, 23, 32, 5, 62];

for (let i = 0; i < randomNumbers.length; i++) {
    console.log(randomNumbers[i]);
}